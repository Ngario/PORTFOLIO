# -------------------------------------------------------------------
# Copy this file to .env for local development. Do NOT commit .env.
# On Render: set these as Environment Variables in the dashboard.
# -------------------------------------------------------------------

# Environment: development | production | testing
CI_ENVIRONMENT=development

# Base URL (with trailing slash). On Render use your service URL.
# app.baseURL=https://your-app.onrender.com/

# -------------------------------------------------------------------
# Database (required for Render so the hosted site uses your DB)
# Use the same variable names in Render Environment tab.
# -------------------------------------------------------------------
# database.default.hostname=your-db-host.example.com
# database.default.database=portfolio_db
# database.default.username=your_user
# database.default.password=your_password
# database.default.port=3306

# -------------------------------------------------------------------
# Optional: show errors on Render (debug 500s)
# SHOW_ERRORS=1
# RENDER_DEBUG=1
